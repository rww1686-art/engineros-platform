APP_NAME=Engineros Platform
APP_ENVIRONMENT=development
# Development-only example. Production MUST inject DATABASE_URL from a secret/config store
# and MUST NOT use the default engineros:engineros credentials.
DATABASE_URL=postgresql+psycopg://engineros:engineros@db:5432/engineros
